# Local development (optional overrides)
# API_PROXY_URL=http://127.0.0.1:8000
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Vercel production: leave NEXT_PUBLIC_API_URL unset (same-origin /api rewrites)

# Python CORS — comma-separated extra origins (custom domain)
# ALLOWED_ORIGINS=https://your-domain.com
